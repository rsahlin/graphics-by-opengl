#version 300 es
/* 
 *
 * Fragment shader for glTF asset
 * @author Richard Sahlin
 */
precision highp float;

void main() {
    fragColor = calculateFresnelDiffuse(normalize((vec3(texture(uTextureNormal, vTexNormal)) * mTangentLight)));
}
